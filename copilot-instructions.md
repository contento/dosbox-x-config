# Copilot Instructions for DOSBox-X Config

This repository contains DOSBox-X configuration defaults and install helper scripts.

> **Sync note**: equivalent guidance for Claude Code lives in `CLAUDE.md`, and ongoing tasks live in `TODO.md`. When you change one of these instruction files, update the other so both assistants stay aligned. Keep `TODO.md` current as work lands or new items surface.

## Project scope
- Primary files:
  - `dosbox-x-global-defaults.md`
  - `DOSBox-X.Preferences`
  - `DOSBox-X.Preferences.initiator`
  - `install-config.sh`
  - `install-config.ps1`
  - `README.md`
- The project is about configuring defaults and installation settings for DOSBox-X.

## How to help
- Make edits only when they are relevant to the config files, install scripts, or documentation.
- Keep changes consistent with DOSBox-X syntax and shell script conventions.
- Ensure solutions remain compatible with Windows, macOS, and Linux.
- Presume the user wants clear, minimal updates and no unrelated refactoring.
- If asked about project-specific behavior, focus on DOSBox-X configuration and install setup.

## README style
- Maintain a standard header and consistent structure in `README.md`, matching the style used across the user's repositories.
- If editing documentation, prefer a concise title, short description, and platform-support sections.

## Communication style
- Answer succinctly and directly.
- Use markdown with headings when explaining changes.
- Highlight the exact files changed.
- Do not assume any additional repo context beyond what is visible in this workspace.
