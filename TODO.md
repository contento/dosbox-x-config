# TODO

- [ ] Add a validation step for installer scripts to confirm the destination path exists before copying.
- [ ] Improve README with a dedicated section on editing and syncing `DOSBox-X.Preferences` across machines.
- [ ] Add platform-specific install examples for both macOS/Linux and Windows with explicit destination override guidance.
- [ ] Consider adding a lightweight test or check script to ensure `install-config.sh` and `install-config.ps1` run correctly.
- [ ] Document the expected DOSBox-X version string and update process for future releases.
